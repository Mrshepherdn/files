# CLAUDE.md

## Project Overview

**Repository:** `files`
**Status:** Initial scaffold — no application code has been added yet.

This repository is a fresh project placeholder. It currently contains only a README.md with a project header.

## Repository Structure

```
files/
├── README.md          # Project readme (header only)
└── CLAUDE.md          # This file — guidance for AI assistants
```

## Development Setup

No build system, package manager, or dependencies are configured yet. When tooling is added, update this section with:

- Language and runtime version requirements
- Install commands (e.g., `npm install`, `pip install -r requirements.txt`)
- Environment variable setup or `.env` configuration

## Build & Run

No build or run commands are configured. Update this section when an application entry point is added.

## Testing

No test framework is configured. Update this section when tests are introduced, including:

- How to run the full test suite
- How to run a single test file
- Any test coverage requirements

## Linting & Formatting

No linters or formatters are configured. Update this section when tools like ESLint, Prettier, Ruff, Clippy, etc. are added.

## CI/CD

No CI/CD pipeline is configured (no GitHub Actions, etc.). Update this section when workflows are added.

## Git Conventions

- **Default branch:** `main` (remote) / `master` (local)
- Write clear, concise commit messages that describe *why* a change was made
- Keep commits focused — one logical change per commit

## Key Conventions for AI Assistants

- Read existing code before proposing changes
- Do not add unnecessary abstractions, features, or over-engineering
- Keep changes minimal and focused on the task at hand
- When adding new tooling or frameworks, update this CLAUDE.md accordingly
- Prefer editing existing files over creating new ones
- Do not introduce security vulnerabilities (validate user input, avoid injection flaws)
